# X Imperium - Docker Compose Configuration
# Usage: docker-compose up -d

version: '3.8'

services:
  web:
    build: .
    container_name: ximperium-web
    ports:
      - "8080:80"
    environment:
      - CONF_DATABASE_DRIVER=mysqli
      - CONF_DATABASE_HOSTNAME=db
      - CONF_DATABASE_USERNAME=ximperium
      - CONF_DATABASE_PASSWORD=${DB_PASSWORD:-ximperium_secret}
      - CONF_DATABASE_NAME=ximperium
      - CONF_TIMEZONE=UTC
    volumes:
      - ./templates_c:/var/www/html/templates_c
    depends_on:
      db:
        condition: service_healthy
    networks:
      - ximperium-network
    restart: unless-stopped

  db:
    image: mysql:8.0
    container_name: ximperium-db
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root_secret}
      MYSQL_DATABASE: ximperium
      MYSQL_USER: ximperium
      MYSQL_PASSWORD: ${DB_PASSWORD:-ximperium_secret}
    volumes:
      - ximperium-db-data:/var/lib/mysql
      - ./sql:/docker-entrypoint-initdb.d:ro
    # Note: Port 3306 not exposed externally for security
    # Use 'docker compose exec db mysql' to access the database
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "ximperium", "-p${DB_PASSWORD:-ximperium_secret}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - ximperium-network
    restart: unless-stopped

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: ximperium-phpmyadmin
    environment:
      PMA_HOST: db
      PMA_USER: ximperium
      PMA_PASSWORD: ${DB_PASSWORD:-ximperium_secret}
    ports:
      - "8081:80"
    depends_on:
      - db
    networks:
      - ximperium-network
    restart: unless-stopped
    profiles:
      - dev

volumes:
  ximperium-db-data:
    driver: local

networks:
  ximperium-network:
    driver: bridge
